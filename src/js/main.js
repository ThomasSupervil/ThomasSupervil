import { MagneticObject } from './magnetize';

// Magnetize elements
const circle = document.querySelector('.magnetize-button');
new MagneticObject(circle, 0.6)

const cross = document.querySelector('.innerCircle');
new MagneticObject(cross, 0.7)


// Cursor styled


